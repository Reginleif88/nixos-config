---
description: Archive and clear the current ISC
allowed-tools: ["Bash(bun run ~/.claude/tools/isc/ISCManager.ts *)"]
---

Archive and clear the current ISC:

```bash
bun run ~/.claude/tools/isc/ISCManager.ts clear
```
